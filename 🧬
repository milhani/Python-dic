import turtle

colores = ['red', 'purple', 'blue',
               'green', 'yellow', 'orange']
turtle.bgcolor('black')
turtle.ht()
for x in range(200):
    turtle.pencolor(colores[x % 6])
    turtle.width(x/100 + 1)
    turtle.forward(x)
    turtle.left(59)
    
